spring:
  application:
    name: ecommerce-backend
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  jackson:
    serialization:
      write-dates-as-timestamps: false
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/multigenesys?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
    username: root
    password: Chetan@123
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8080

app:
  jwt:
    secret: change-this-before-production-change-this-before-production
    expiration-ms: 86400000
  stripe:
    secret-key: change-this-before-production
    webhook-secret: change-this-before-production
